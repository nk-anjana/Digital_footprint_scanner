SECRET_KEY=run-openssl-rand-hex-32-and-paste-here
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# Set to "production" for production environments to enable secure cookies, etc.
ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root@123
DB_NAME=osint_db

REDIS_URL=redis://localhost:6379/0

ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8501
```

---

## `requirements.txt`
```
fastapi>=0.110.0
uvicorn[standard]
pymysql
passlib[bcrypt]
python-jose[cryptography]
celery
redis
requests
pillow
python-dotenv
slowapi
python-multipart